import { connectionDB } from "../config/database.js";

export default {
    async create({ nome, cnpj, telefone, email }) {
        try {
            // const existEmail =
        } catch (error) {}
    },
};

